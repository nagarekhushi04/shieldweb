[build]
builder = "nixpacks"
buildCommand = "npm ci && cd packages/api && npm run build"

[deploy]
startCommand = "cd packages/api && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on-failure"
